# If BASE_URL is set, tests point at this URL directly (e.g. a deployed instance).
# If left unset, conftest.py spins up a local static server for /webapp and uses that instead.
# BASE_URL=http://localhost:5500

HEADLESS=true
BROWSER=chromium
